# ✅ Supabase MCP - Настройка Завершена!

## 🎉 Всё готово к работе!

MCP сервер успешно развернут, настроен и протестирован.

### Что установлено

#### На сервере (31.129.98.96)
- ✅ **MCP Server с HTTP wrapper** - контейнер `supabase-mcp` работает на порту 8000
- ✅ **Kong Gateway** - проксирует `/mcp` → `http://mcp-server:8000`
- ✅ **Все зависимости** - Supabase DB, Auth, Rest, Storage

#### На локальной машине
- ✅ **SSH туннель** - `localhost:8080` → `31.129.98.96:8000`
- ✅ **Claude Code настроен** - `~/.config/claude-code/settings.json`
- ✅ **Endpoint протестирован** - работает корректно

---

## 🚀 Как использовать

### Шаг 1: Убедитесь что SSH туннель работает

```bash
ps aux | grep "ssh.*8080"
```

Если нет - запустите:
```bash
ssh -N -L localhost:8080:localhost:8000 root@31.129.98.96 &
```

### Шаг 2: Перезапустите Claude Code

Полностью закройте и откройте Claude Code заново чтобы применить конфигурацию MCP.

### Шаг 3: Проверьте работу

Откройте Claude Code и спросите:

```
Какие таблицы есть в моей Supabase БД? Используй MCP tools.
```

Claude должен использовать MCP и показать список таблиц.

---

## 📋 Доступные MCP Tools

MCP предоставляет **22 инструмента** для работы с Supabase:

### База данных
- `list_tables` - Список таблиц
- `execute_sql` - Выполнение SQL запросов
- `list_extensions` - Установленные расширения
- `get_database_stats` - Статистика БД
- `get_database_connections` - Активные подключения

### Миграции
- `list_migrations` - Список миграций
- `apply_migration` - Применить миграцию

### Аутентификация
- `list_auth_users` - Список пользователей
- `get_auth_user` - Получить пользователя
- `create_auth_user` - Создать пользователя
- `update_auth_user` - Обновить пользователя
- `delete_auth_user` - Удалить пользователя

### Storage
- `list_storage_buckets` - Список buckets
- `list_storage_objects` - Объекты в bucket

### Конфигурация
- `get_project_url` - URL проекта
- `get_anon_key` - Anon ключ
- `get_service_key` - Service role ключ
- `verify_jwt_secret` - Проверка JWT secret

### Дополнительно
- `generate_typescript_types` - Генерация TypeScript типов
- `list_realtime_publications` - Realtime публикации
- `rebuild_hooks` - Перезапуск pg_net worker

---

## 🔧 Управление

### Проверка статуса

```bash
# Статус MCP контейнера
ssh root@31.129.98.96 "docker ps | grep mcp"

# Логи MCP
ssh root@31.129.98.96 "docker logs supabase-mcp --tail 50"

# Проверка SSH туннеля
ps aux | grep "ssh.*8080"
```

### Перезапуск компонентов

```bash
# Перезапуск MCP сервера
ssh root@31.129.98.96 "cd /root/supabase && docker compose restart mcp-server"

# Перезапуск SSH туннеля
pkill -f "ssh.*8080"
ssh -N -L localhost:8080:localhost:8000 root@31.129.98.96 &
```

### Тестирование endpoint

```bash
curl -X POST 'http://localhost:8080/mcp' \
  -H 'Content-Type: application/json' \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}}}' | jq '.result.capabilities.tools | keys'
```

---

## 📁 Конфигурация

### Claude Code (`~/.config/claude-code/settings.json`)

```json
{
  "mcpServers": {
    "supabase-herdmaster": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "http://localhost:8080/mcp"
      ],
      "description": "Self-hosted Supabase MCP server for HerdMaster Pro"
    }
  }
}
```

### SSH Туннель

```bash
ssh -N -L localhost:8080:localhost:8000 root@31.129.98.96
```

Для постоянного туннеля можно использовать `autossh` или systemd service.

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────┐
│ Локальная машина                            │
│                                             │
│  Claude Code                                │
│    ↓ (npx mcp-remote)                       │
│  http://localhost:8080/mcp                  │
│    ↓ (SSH tunnel)                           │
└──────────────┬──────────────────────────────┘
               │
               ↓ ssh -L 8080:8000
┌──────────────┴──────────────────────────────┐
│ Сервер 31.129.98.96                         │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │ Kong (:8000)                           │ │
│  │   ↓ /mcp                               │ │
│  │ supabase-mcp (:8000)                   │ │
│  │   - HTTP wrapper (Node.js)             │ │
│  │   - MCP server (stdio → HTTP bridge)   │ │
│  │   ↓                                    │ │
│  │ supabase-db (PostgreSQL)               │ │
│  │ supabase-rest (PostgREST)              │ │
│  │ supabase-auth (GoTrue)                 │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

---

## 🛠️ Troubleshooting

### MCP не отвечает

1. Проверьте SSH туннель: `ps aux | grep ssh.*8080`
2. Проверьте MCP контейнер: `ssh root@31.129.98.96 "docker ps | grep mcp"`
3. Логи MCP: `ssh root@31.129.98.96 "docker logs supabase-mcp"`

### Claude Code не видит MCP

1. Проверьте конфигурацию: `cat ~/.config/claude-code/settings.json`
2. Перезапустите Claude Code полностью
3. Проверьте что `npx` установлен: `npx --version`

### Ошибки SQL

Убедитесь что функция `execute_sql` создана в БД:
```sql
-- Проверка
SELECT proname FROM pg_proc WHERE proname = 'execute_sql';
```

---

## 📚 Документация

- **Быстрый старт**: `docs/QUICKSTART-MCP.md`
- **Подробная инструкция**: `docs/MCP-FINAL-SETUP.md`
- **Официальная документация**: [Supabase MCP](https://supabase.com/docs/guides/self-hosting/enable-mcp)

---

## ✅ Чеклист готовности

- [x] MCP сервер запущен и работает
- [x] HTTP wrapper принимает запросы
- [x] Kong проксирует `/mcp` endpoint
- [x] SSH туннель активен
- [x] Claude Code настроен
- [x] Endpoint протестирован
- [x] Документация создана

---

## 🎊 Готово!

**Статус:** ✅ Production Ready

**Версия MCP:** HenkDz/selfhosted-supabase-mcp + custom HTTP wrapper

**Дата:** 24 января 2026

**Следующий шаг:** Перезапустите Claude Code и начните работу с Supabase через natural language!

---

**Создано:** Claude Sonnet 4.5
**Проект:** HerdMaster Pro
