# HerdMaster Pro CLI - Руководство по тестированию

## Текущие тестовые данные

В базе данных загружено **29 животных** для тестирования всех RC кодов:

| RC | Статус | Количество | ID животных |
|----|--------|------------|-------------|
| 0 | BLANK | 6 | 1008, 1009, 1010, 1011, 1012, 1013 |
| 1 | DNB | 2 | 1014, 1015 |
| 2 | FRESH | 4 | 1004, 1005, 1016, 1017 |
| 3 | OPEN | 5 | 1001, 1018, 1019, 1020, 1021 |
| 4 | BRED | 4 | 1002, 1022, 1023, 1024 |
| 5 | PREG | 5 | 1003, 1007, 1025, 1026, 1027 |
| 6 | DRY | 3 | 1006, 1028, 1029 |

## Тестовые команды

### Базовые команды по RC коду

#### 1. Список телок без статуса (RC=0)
```bash
LIST ID FOR RC=0
```
**Ожидаемый результат:** 6 животных (1008-1013)

#### 2. Коровы DNB - на выбраковку (RC=1)
```bash
LIST ID PEN LACT FOR RC=1
```
**Ожидаемый результат:** 2 коровы (1014, 1015)

#### 3. Свежие коровы (RC=2)
```bash
LIST ID PEN LACT DIM FOR RC=2
```
**Ожидаемый результат:** 4 коровы (1004, 1005, 1016, 1017)

#### 4. Открытые коровы - готовые к осеменению (RC=3)
```bash
LIST ID PEN LACT DIM FOR RC=3
```
**Ожидаемый результат:** 5 коров (1001, 1018-1021)

#### 5. Осемененные коровы (RC=4)
```bash
LIST ID PEN LACT DIM FOR RC=4
```
**Ожидаемый результат:** 4 коровы (1002, 1022-1024)

#### 6. Стельные коровы (RC=5)
```bash
LIST ID PEN LACT FOR RC=5
```
**Ожидаемый результат:** 5 коров (1003, 1007, 1025-1027)

#### 7. Запущенные коровы (RC=6)
```bash
LIST ID PEN FOR RC=6
```
**Ожидаемый результат:** 3 коровы (1006, 1028, 1029)

### Комбинированные фильтры

#### 8. Свежие коровы старше 21 дня (готовы к переводу в OPEN)
```bash
LIST ID PEN DIM FOR RC=2 DIM>21
```
**Логика:** Показывает свежих коров, прошедших добровольный период ожидания

#### 9. Открытые коровы с высокой лактацией
```bash
LIST ID PEN LACT DIM FOR RC=3 LACT>2
```
**Логика:** Коровы в 3+ лактации, приоритет для осеменения

#### 10. Осемененные коровы для проверки стельности
```bash
LIST ID PEN LACT FOR RC=4
```
**Логика:** Все коровы RC=4 должны быть проверены на 35-45 день после AI

#### 11. Не-стельные коровы (нужно внимание)
```bash
LIST ID PEN LACT FOR RC<5
```
**Логика:** Все коровы кроме PREG и DRY - требуют действий

### Производственные отчеты

#### 12. "To Breed" список (приоритет на сегодня)
```bash
LIST ID PEN LACT DIM FOR RC=3 DIM>60
```
**Назначение:** Коровы готовые к осеменению прямо сейчас

#### 13. "Preg Check" список
```bash
LIST ID PEN LACT FOR RC=4
```
**Назначение:** Коровы для проверки стельности

#### 14. Все животные в активном воспроизводстве
```bash
LIST ID PEN LACT FOR RC>=2 RC<=5
```
**Логика:** FRESH → OPEN → BRED → PREG (активный цикл)

#### 15. Проблемные животные
```bash
LIST ID PEN LACT FOR RC=1
```
**Назначение:** Коровы DNB - нужно выяснить причину

### Тесты на сортировку и множественные условия

#### 16. Стельные коровы, сортировка по лактации
```bash
LIST ID LACT FOR RC=5 SORT LACT
```
**Ожидается:** Сортировка по возрастанию лактации

#### 17. Открытые коровы, высокий надой
```bash
LIST ID MILK FOR RC=3 MILK>35
```
**Логика:** Высокопродуктивные коровы готовые к осеменению

#### 18. Несколько условий одновременно
```bash
LIST ID PEN LACT DIM FOR RC=3 LACT>1 DIM>60
```
**Логика:** Опытные коровы (LACT>1) готовые к осеменению (DIM>60)

### Подсчет и аналитика

#### 19. Подсчет животных по группам
```bash
LIST ID FOR RC=0
LIST ID FOR RC=2
LIST ID FOR RC=3
LIST ID FOR RC=4
LIST ID FOR RC=5
LIST ID FOR RC=6
```
**Назначение:** Получить количество в каждой группе

#### 20. Распределение стада
```bash
# Heifers
LIST ID FOR RC=0

# Lactating
LIST ID FOR RC>=2 RC<=5

# Dry
LIST ID FOR RC=6
```

### Интеграция с GUI

При выполнении этих команд должна подсвечиваться соответствующая секция в боковой панели:

| Команда | Подсветка Sidebar |
|---------|-------------------|
| `LIST ID FOR RC=2` | **Fresh Cows** |
| `LIST ID FOR RC=3 DIM>60` | **To Breed** |
| `LIST ID FOR RC=4` | **Pregnancy Check** |
| `LIST ID FOR RC=5 DCC<60` | **Dry Off** |

### Ожидаемое поведение CLI

1. **Фокус:**
   - Нажатие `/` или `Ctrl+L` → фокус на CLI
   - CLI подсвечивается зеленой рамкой

2. **Подсветка синтаксиса:**
   - Команда: зеленый (`LIST`, `SHOW`)
   - Поля: синий (`ID`, `PEN`, `LACT`)
   - Операторы: серый (`FOR`, `SORT`)
   - Числа: желтый (`2`, `60`)

3. **Автодополнение:**
   - При вводе `LI` → предлагает `LIST`
   - При вводе `ID` → предлагает `ID`, `DIM`, `DCC`

4. **История:**
   - `↑` / `↓` - навигация по истории команд
   - История сохраняется в сессии

5. **Результаты:**
   - Появляется панель с результатами
   - Таблица с колонками
   - RC коды отображаются с цветными бейджами
   - Toast уведомление: "Found X results in Yms"

6. **Ошибки:**
   - Синтаксическая ошибка → Toast: "Parse error"
   - Ошибка выполнения → Toast: "Execution error"
   - Подробное описание в описании toast

### Проверка RC кодов

#### Визуальная проверка RC badges:

RC коды должны отображаться с цветными бейджами:

- RC=0 (BLANK): серый
- RC=1 (DNB): красный
- RC=2 (FRESH): синий
- RC=3 (OPEN): желтый
- RC=4 (BRED): оранжевый
- RC=5 (PREG): зеленый
- RC=6 (DRY): фиолетовый
- RC=7 (SOLD): серый темный
- RC=8 (BULLCALF): розовый

### Тестирование производительности

```bash
# Большой запрос - все животные
LIST ID PEN LACT DIM

# Должно выполниться < 500ms
```

### Негативные тесты

#### 21. Несуществующий RC код
```bash
LIST ID FOR RC=15
```
**Ожидается:** 0 результатов (нет животных с RC=15)

#### 22. Неверный синтаксис
```bash
LIST FOR ID RC=5
```
**Ожидается:** Parse error

#### 23. Несуществующее поле
```bash
LIST UNKNOWN FOR RC=5
```
**Ожидается:** Игнорирование неизвестного поля или ошибка

## Отладка

### Логи в консоли браузера

Откройте DevTools (F12) → Console:

```javascript
// При выполнении команды должны быть логи:
// 1. Парсинг команды
// 2. Конвертация RC код → reproductive_status
// 3. SQL запрос
// 4. Результаты
```

### Проверка сетевых запросов

DevTools → Network → Filter: `animals`:

```
Request URL: https://herd.b2bautomate.ru/rest/v1/animals?select=...&reproductive_status=eq.preg
```

### Проверка state

React DevTools → Components → CliResultsProvider:

```javascript
{
  result: {...},
  command: "LIST ID FOR RC=5",
  isVisible: true
}
```

## Скрипты для тестирования

### Проверить распределение животных
```bash
cd apps/web
bash scripts/check-animals.sh
```

### Загрузить дополнительные тестовые данные
```bash
cd apps/web
bash scripts/load-extended-test-data.sh
```

### Очистить тестовые данные
```bash
# TODO: создать скрипт для очистки
```

## Известные ограничения

1. **RC=7 (SOLD)** и **RC=8 (BULLCALF)** - не загружены из-за check constraints на `current_status`
2. **DIM** и **DCC** - рассчитываемые поля, требуют добавления в view
3. **SORT** - базовая реализация, только по одному полю

## Следующие шаги

1. ✅ Исправить маппинг RC кодов
2. ✅ Загрузить тестовые данные для RC=0..6
3. 🔄 Добавить calculated fields (DIM, DCC) в database view
4. 🔄 Реализовать COUNT, SUM команды
5. 🔄 Добавить поддержку OR условий
6. 🔄 Реализовать UPDATE, ADD команды

## Полезные ссылки

- [CLI RC Codes Guide](./CLI-RC-CODES-GUIDE.md) - Подробное руководство по RC кодам
- [RC Code Fix](./RC-CODE-FIX.md) - Техническая документация по исправлению
- [Field Mapping](../apps/web/src/lib/cli/field-mapping.ts) - Маппинг полей DairyComp → DB
