# Быстрый старт: Supabase MCP

Краткая инструкция по настройке MCP сервера для HerdMaster Pro.

## Шаг 1: Настройка на сервере (31.129.98.96)

Скопируйте и запустите скрипт на сервере:

```bash
# Скопировать скрипт на сервер
scp scripts/setup-mcp-server.sh root@31.129.98.96:/tmp/

# Подключиться к серверу
ssh root@31.129.98.96

# Запустить настройку
chmod +x /tmp/setup-mcp-server.sh
/tmp/setup-mcp-server.sh
```

Скрипт автоматически:
- Определит Docker bridge gateway IP
- Найдет и обновит kong.yml
- Перезапустит Kong контейнер

## Шаг 2: Запуск SSH туннеля (на локальной машине)

```bash
# Из корневой директории проекта
./scripts/start-mcp-tunnel.sh
```

Или вручную:
```bash
ssh -N -L localhost:8080:localhost:8000 root@31.129.98.96
```

Оставьте терминал с туннелем открытым.

## Шаг 3: Проверка работы (на локальной машине)

```bash
./scripts/test-mcp-local.sh
```

Должен вывести "All tests passed!".

## Шаг 4: Настройка Claude Code

### Вариант 1: Автоматическая настройка

```bash
# Скопировать конфигурацию MCP
mkdir -p ~/.config/claude-code
cp .claude/mcp-config.json ~/.config/claude-code/settings.json
```

### Вариант 2: Ручная настройка

Откройте файл настроек:
```bash
# Linux/macOS
nano ~/.config/claude-code/settings.json

# macOS (альтернативный путь)
nano ~/Library/Application\ Support/claude-code/settings.json
```

Добавьте:
```json
{
  "mcpServers": {
    "supabase-herdmaster": {
      "url": "http://localhost:8080/mcp"
    }
  }
}
```

Перезапустите Claude Code.

## Шаг 5: Тест в Claude Code

Откройте Claude Code и спросите:

```
Какой у меня Supabase anon key? Используй инструменты MCP сервера.
```

Claude должен использовать MCP tools и получить информацию.

## Готово! 🎉

Теперь Claude Code может работать с вашим Supabase через MCP:
- Читать конфигурацию проекта
- Выполнять SQL запросы
- Управлять миграциями
- Просматривать схему БД

## Устранение проблем

### MCP сервер недоступен
```bash
# Проверить SSH туннель
ps aux | grep "ssh.*8080"

# Проверить Kong на сервере
ssh root@31.129.98.96 "docker ps | grep kong"
```

### Ошибка 403
Повторно запустите setup-mcp-server.sh на сервере.

### Claude Code не видит MCP
1. Проверьте путь к settings.json
2. Убедитесь в корректности JSON
3. Перезапустите Claude Code полностью

## Дополнительная информация

Полная документация: `docs/supabase-mcp-setup.md`
